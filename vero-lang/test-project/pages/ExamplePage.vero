# Example.com Page - Field definitions only (protected)
# Access these fields through ExamplePageActions

PAGE ExamplePage {
    FIELD heading = "h1"
    FIELD learnMoreLink = "a:has-text('More information')"
}

# Google Search Page

page SearchHomePage {
    field searchInput = "[name='q']"
    field searchResults = "#search"

    searchFor with query {
        fill searchInput with query
        press "Enter"
    }
}

# SearchHomePage - Field definitions only (protected)
# Access these fields through SearchHomePageActions

PAGE SearchHomePage {
    FIELD searchInput = "[name='q']"
    FIELD searchResults = "#search"
}

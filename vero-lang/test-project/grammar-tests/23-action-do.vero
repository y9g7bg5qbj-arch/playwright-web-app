# Test: PERFORM Action Variations
# Category: Actions - PERFORM

PAGE LoginPage {
    FIELD emailInput = "Email"
    FIELD passwordInput = "Password"
    FIELD submitBtn = "Submit"

    login WITH email, pwd {
        FILL emailInput WITH email
        FILL passwordInput WITH pwd
        CLICK submitBtn
    }

    quickLogin WITH email {
        FILL emailInput WITH email
        CLICK submitBtn
    }

    logout {
        CLICK "Logout"
    }
}

FEATURE PerformActions {
    USE LoginPage

    SCENARIO PerformActionVariations {
        # PERFORM without arguments
        PERFORM LoginPage.logout

        # PERFORM with single argument
        PERFORM LoginPage.quickLogin WITH "test@example.com"

        # PERFORM with multiple arguments
        PERFORM LoginPage.login WITH "admin@example.com", "password123"

        # PERFORM with variable arguments
        TEXT email = "user@example.com"
        TEXT pwd = "secret"
        PERFORM LoginPage.login WITH email, pwd
    }
}

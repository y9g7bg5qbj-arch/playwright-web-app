# Example.com Feature

feature Example {
    use ExamplePage
    
    before each {
        open "https://example.com"
    }
    
    scenario "Click learn more link" @smoke {
        verify ExamplePage.heading is visible
        click ExamplePage.learnMoreLink
        wait 2 seconds
    }
}
